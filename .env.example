# Firebase Configuration (Client-side)
# Get these values from Firebase Console > Project Settings > General > Your apps
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_PROJECT_ID=your_project_id_here
VITE_FIREBASE_APP_ID=your_app_id_here

# Firebase Admin SDK (Server-side)
# Get this from Firebase Console > Project Settings > Service Accounts > Generate new private key
# Path to your Firebase Admin SDK service account JSON file
FIREBASE_ADMIN_SERVICE_ACCOUNT_PATH=./firebase-admin-key.json

# Optional: Firebase Admin credentials as JSON string (for deployment environments)
# FIREBASE_ADMIN_CREDENTIALS={"type":"service_account","project_id":"..."}

# Application Configuration
NODE_ENV=development
PORT=5000
